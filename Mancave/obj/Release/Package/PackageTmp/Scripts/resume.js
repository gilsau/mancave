$(document).ready(function () {
    /*
    $('#frmResume').submit(function(e){
        e.stopPropagation();
        e.preventDefault();

        //If no skill provided, send empty values to server-side, so controller will work
        if ($.trim($('#Skill').val()) == '') {

        }
    });*/
    
});